# Postgres connection string.
# Local dev: run `npx prisma dev` and copy the DATABASE_URL it prints into your .env.
# Home server: points at the existing Postgres server + this app's dedicated database.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/test_taking_app

# Port the Next.js server listens on.
PORT=3000

NODE_ENV=development
